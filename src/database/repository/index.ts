/**
 * Database repository module exports
 * Provides high-level singleton repository for database operations
 */

export { Repository, RepositoryConfig } from './types';
export { repository } from './repository';
