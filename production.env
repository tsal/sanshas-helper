# Production Environment Configuration
# Set these environment variables in your production deployment environment

# Discord Bot Configuration
DISCORD_TOKEN=your_production_bot_token

# Bot Installation
# The bot needs permissions for accessing messages and roles to function properly.
# Use this URL format to install the bot in your Discord server:
# https://discord.com/oauth2/authorize?client_id=YOUR_CLIENT_ID&permissions=2415921152&scope=bot%20applications.commands

# Role Configuration
# Comma-separated list of roles to enable for your specific community
# Leave commented out to enable all roles, or specify only the roles your community uses
# Valid roles: Exploration, Industry, Mining, PVE, PVP
TRIBE_ROLES=Exploration,Industry,Mining,PVE,PVP

# Response Theme Configuration (optional)
# Sets the personality/messaging theme for the bot responses
# Valid themes: kuvakei (Sansha's Nation), triglav (Triglavian Collective)
# Default: kuvakei
# RESPONSE_THEME=kuvakei

# Roles Command Name Configuration (optional)
# Sets the name for the role selection slash command
# Must be a single word, lower-case is best, any characters that Discord supports for slash command names are allowed
# Default: eve-roles
# ROLES_COMMAND_NAME=eve-roles

# Database Configuration (optional)
# Path to the database file for persistent storage
# Enable this in production if you want to persist data across bot restarts
# Leave commented out to disable database features
# DATABASE_PATH=/app/data/db.json

# Intel Expiration Configuration (optional)
# Default expiration time for intel items in hours
# Must be a non-negative number
# Default: 168 hours
# DEFAULT_INTEL_EXPIRATION=168
